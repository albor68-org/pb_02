#ifndef MEDIAN_H      // Проверяем, объявлен ли уже этот файл
#define MEDIAN_H      // Если нет, объявляем

#include <vector>

// Функция для вычисления медианы
double median(std::vector<double> vec);

#endif // MEDIAN_H